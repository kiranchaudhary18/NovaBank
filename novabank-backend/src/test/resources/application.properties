# ===================================================================
# NovaBank Backend Test Properties
# ===================================================================

# Disable JPA Auditing for tests
novabank.jpa.auditing.enabled=false

# Disable Hibernate DDL execution to prevent connection attempts during startup
spring.jpa.hibernate.ddl-auto=none
